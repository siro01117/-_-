import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './SCP.jsx' // 깃허브에 올린 파일 이름이 SCP.jsx이므로 맞춤

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)